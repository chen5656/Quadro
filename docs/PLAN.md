# NODRA 智能对战系统 — 总体计划书（修订版 v3）

> **NODRA** 是本项目的自有名称与规则实现。项目使用一套自有术语（见第 1 节术语表），不使用任何第三方桌游的商标名、美术资产或包装措辞。
>
> 各模块详细计划见 `docs/plans/`：
>
> - [01-engine.md](docs/plans/01-engine.md) — 规则引擎 + CLI 验证工具
> - [02-slice.md](docs/plans/02-slice.md) — 最小服务端 + 最小 Web 客户端（可玩垂直切片）
> - [03-ai.md](docs/plans/03-ai.md) — AI 代理与基准测试
> - [04-frontend.md](docs/plans/04-frontend.md) — 完整前端
> - [05-integration.md](docs/plans/05-integration.md) — 联调、回放与验收

## 0. 相对上一版（v2）的修订

| # | 修订 | 原因 |
|---|------|------|
| A | **项目更名为 NODRA**，并替换全部专有术语为自有命名（见 1.1 术语表） | 规避商标 |
| B | **路线图改为"薄垂直切片优先"**：Day 3 有可玩 CLI，Day 6 有可玩网页，AI 每落地一级即可人工对打 | 原计划到 Day 9 才有界面，用户无法验证引擎正确性与 AI 强度，反馈太晚 |
| C | AI 阶段后移至切片之后，但**每个 Agent 落地即接入网页**，边做边验 | 同上 |
| D | 新增"人工验证清单"作为每阶段出口条件之一，与自动化测试并列 | 规则细节的错（如连击计分、先手标记）单测容易写成"和实现一样错"，需人眼过 |

### v2 相对最初稿的修订（保留备查）

| # | 修订 | 原因 |
|---|------|------|
| 1 | 补充袋子/弃料盒循环规则、展台不满员规则 | 初稿遗漏，引擎与 AI 模拟必需 |
| 2 | 补充"主动弃料到罚分行"为合法动作；补充备料行锁色规则 | 影响合法动作生成 |
| 3 | 先手标记占罚分行槽位计 -1，但永不进弃料盒 | 规则细节 |
| 4 | 补充平局判定（完整横行数 → 共同胜利） | 初稿遗漏 |
| 5 | AI 定性修正：本游戏是**完美信息 + 随机性**博弈（袋内构成可推算，仅抽取顺序随机），轮内为纯完美信息。Level 3 用**确定化 MCTS**，不用 ISMCTS | 初稿定性错误 |
| 6 | 删除 Numba；状态用紧凑数组 + 增量评估 | 过早优化 |
| 7 | Next.js → Vite + React；音效降为可选 | 纯客户端界面无需 SSR |
| 8 | 联机房间 + 断线恢复降为可选扩展；PvP 先做同屏热座 | 缩减 MVP 范围 |

## 1. 游戏规格

### 1.1 术语表（全项目唯一命名标准）

| 本项目术语 | 英文标识符 | 含义 |
|-----------|-----------|------|
| 方块 | `tile` | 5 色游戏组件，每色 20 块，共 100 块 |
| 展台 | `display` / `displays` | 每轮摆放 4 块方块的公共取料位，2 人局共 5 个 |
| 中央池 | `center` | 展台余料的汇集处 |
| 先手标记 | `first_token` | 决定下轮先手的标记物，编码为 `-1` |
| 备料行 | `staging_rows` | 玩家板左侧 5 行，容量 1..5，同行锁定单色 |
| 网格 | `grid` | 玩家板右侧 5×5 结算区 |
| 罚分行 | `penalty_row` | 玩家板底部 7 槽，放置溢出与弃置方块 |
| 弃料盒 | `discard` | 已结算方块的回收处，袋空时倒回袋中 |
| 取料阶段 | `drafting` | 轮内玩家交替取方块的阶段 |
| 结算阶段 | `settling` | 轮末上格计分与罚分的阶段 |

代码、协议、UI 文案一律使用上表标识符；不得出现其他桌游的专有名词。

### 1.2 规格摘要

完整规格（含全部边界判例）见 [01-engine.md](docs/plans/01-engine.md)。

- **人数**：2 人（5 个展台，每展台 4 块）。
- **方块**：5 色（BLUE, YELLOW, RED, BLACK, WHITE）各 20 块，共 100 块；另有 1 枚先手标记。
- **网格色位**：`color(r, c) = (c - r + 5) mod 5`（第 0 行为 蓝黄红黑白，每行右移一位）。
- **备料行**：容量 1..5；每行锁定单色；网格该行已有该色则该行不可选该色。
- **罚分行**：7 槽，罚分 `-1,-1,-2,-2,-2,-3,-3`；溢出方块进弃料盒；总分不低于 0。
- **取料**：从某展台或中央池取走某色全部方块；展台余料入中央池；首个从中央池取料者获得先手标记并置于罚分行；取得的方块放入一条备料行（溢出落罚分行）**或整组直接弃入罚分行**。
- **结算**：满行取 1 块上网格，余料入弃料盒；新方块得分 = 水平连续段长 H（若 H≥2）+ 垂直连续段长 V（若 V≥2），两者皆为 1 时得 1 分。
- **补料**：每轮开始从袋中抽料填展台；袋空则将弃料盒洗回袋中继续；两者皆空则展台不满员照常进行。
- **终局**：结算阶段后任一玩家网格出现完整横行即终局。加分：完整横行 +2/行，完整竖列 +7/列，集齐同色 5 块 +10/色。
- **胜负**：分高者胜；平分比完整横行数；再平则共同获胜。

## 2. 系统架构与技术栈

```
+------------------------------------------------------------------+
|                    Frontend (Vite + React + TS)                  |
|  - Tailwind CSS，DOM/SVG 渲染（无需 Canvas）                      |
|  - WebSocket 连接后端；两步交互（选料 → 选行）                     |
+--------------------------------+---------------------------------+
                                 | WebSocket + 少量 REST
+--------------------------------v---------------------------------+
|                       Python Backend (FastAPI)                   |
|  - Game Manager：内存中管理对局（PvP 热座 / PvE / EvE）           |
|  - NODRA Engine：纯 Python，紧凑数组状态，确定性种子              |
|  - AI Service：Random / Greedy / Minimax / MCTS，异步执行         |
+------------------------------------------------------------------+
                                 ^
                    另有 CLI 直连引擎（cli.py / benchmark.py）
```

**依赖原则**：`engine` 零第三方依赖；`ai` 只依赖 `engine`；`app` 依赖两者；CLI 工具直连 `engine`，不经服务端。前端只与 JSON 协议对话。

### 2.1 对战模式

| 模式 | 玩家 1 | 玩家 2 | 实现 |
|------|--------|--------|------|
| PvP | 人类 | 人类 | 同屏热座（单客户端控制双方）；联机房间为可选扩展 |
| PvE | 人类 | AI（可选难度） | WebSocket 即时响应，AI 决策 < 500ms |
| EvE | AI A | AI B | 网页可调播放速度观战；另有 CLI 批量基准 |

## 3. 目录结构

```
NODRA/
├── backend/
│   ├── pyproject.toml
│   ├── app/
│   │   ├── main.py              # FastAPI 入口、REST + WebSocket 路由
│   │   ├── game_manager.py      # 对局生命周期、AI 调度、并发控制
│   │   └── schemas.py           # Pydantic 模型（与前端协议一一对应）
│   ├── engine/
│   │   ├── constants.py         # 颜色、罚分表、网格色位等常量
│   │   ├── state.py             # GameState / PlayerBoard 数据结构与序列化
│   │   ├── rules.py             # 合法动作生成、动作应用、轮末结算、终局判定
│   │   └── game.py              # NODRA 游戏驱动器：洗袋、发展台、回合循环、事件流
│   ├── ai/
│   │   ├── base.py  random_agent.py  greedy_agent.py
│   │   ├── minimax_agent.py  mcts_agent.py
│   │   └── evaluate.py          # 共享评估函数
│   ├── scripts/
│   │   ├── cli.py               # 终端可玩客户端 + ASCII 渲染（Phase 1 交付）
│   │   ├── benchmark.py         # EvE 批量对战，输出胜率/耗时报告
│   │   └── replay.py            # 对局记录回放校验
│   └── tests/
├── frontend/
│   └── src/{components,hooks,types}/ + App.tsx
└── docs/
    ├── plans/                   # 各模块详细计划
    ├── protocol.md              # 通信协议规范
    └── ai_benchmarks.md         # AI 基准结果
```

## 4. AI 分级（概要）

- **Level 0 Random**：均匀随机合法动作，基线与测试对照。
- **Level 1 Greedy**：一步模拟 + 共享评估函数取最优。
- **Level 2 Minimax + α-β**：轮内为纯完美信息博弈，深度 3~5 交替搜索，Greedy 分数做走法排序。
- **Level 3 确定化 MCTS**：UCT；跨轮时对洗袋结果采样确定化；rollout 用轻量 Greedy；预算 500ms。

详见 [03-ai.md](docs/plans/03-ai.md)。

## 5. 路线图（v3 — 垂直切片优先）

**核心原则：每个阶段结束时，你都能亲手玩到当前成果并判断对错。**

| 阶段 | 周期 | 交付 | 你能验证什么 | 出口条件 |
|------|------|------|-------------|----------|
| **P1 引擎 + CLI** | Day 1-3 | `engine/` + `scripts/cli.py`（终端可玩，ASCII 棋盘） | **终端里完整打一局**，逐步核对计分、先手标记、溢出、洗袋是否符合规则 | 全部边界判例单测通过；1000 局随机自对战不变量无违反；同种子完全可复现；**你人工打完 ≥1 局确认规则正确** |
| **P2 可玩切片** | Day 4-6 | 最小 FastAPI + 最小 Web UI（PvE 对 Random/Greedy） | **浏览器里打一局**，验证交互手感与协议 | 浏览器完整跑通 PvE 一局；非法动作被拦截；协议文档成型 |
| **P3 AI 分级** | Day 7-9 | Greedy → Minimax → MCTS，逐个落地并接入网页；`benchmark.py` | **每级 AI 落地当天你就能在网页上打它**，主观判断棋力，再对照胜率数据 | 胜率矩阵达标（见 03 号计划）；每步 < 500ms；**你主观确认难度梯度合理** |
| **P4 完整前端** | Day 10-12 | 动画、落点预览、动作日志、EvE 观战、热座 PvP、终局弹窗 | 完整产品手感 | 三模式全流程顺畅；验收清单逐条通过 |
| **P5 联调与回放** | Day 13-14 | 对局记录导出/回放校验、README、CI | 导出对局并重放 | 记录可导出且重放一致；一条命令跑全部测试 |

**关键变化**：AI 不再是最后才验证的黑盒。P3 里三级 AI 是**串行落地**的——Greedy 做完立刻接进 P2 的网页让你打，觉得太弱/太怪就当场调评估函数，再进 Minimax。评估函数的权重调参靠"你的主观棋感 + benchmark 胜率"双通道反馈，比纯数据调参可靠得多。

### v1.0.0 — 浏览器版与每日挑战（TypeScript）

在 P1–P5 之上新增的一期，完整规格见
[docs/working-backwards/web-ts-daily-challenge/BUILD-SPEC.md](docs/working-backwards/web-ts-daily-challenge/BUILD-SPEC.md)。

| 步骤 | 交付 | 出口条件 |
|------|------|---------|
| 1 引擎移植 | `web/src/engine/` | 对拍向量逐步深比对全绿；2000 局模糊测试不变量无违反 |
| 2 AI 移植 | `web/src/ai/` | 四个等级可用；`npm run bench` 复现 `docs/ai_benchmarks.md` 的强度排序 |
| 3 Practice | `web/src/routes/Practice.tsx` | 断网可完整对局；不发任何请求 |
| 4 Daily + 计时 | `web/src/routes/Daily.tsx` | 同一 `puzzle_id` 全设备同一开局；仅胜局可提交 |
| 5 Worker + D1 + Clerk | `web/worker/` | 端点鉴权、BR-011/012/013 拒绝、最优成绩 upsert 全部有测试 |
| 6 排行榜 | `web/src/components/Leaderboard.tsx` | 未登录可读；离线为状态而非错误 |
| 7 离线与 Service Worker | `vite-plugin-pwa` | 首次加载后断网仍可 Practice；对局进行中不弹重载 |

`backend/` 保留为参考实现与对拍向量来源，不在运行路径上；`frontend/` 由
`web/` 取代。

可选扩展（不阻塞验收）：联机房间与断线恢复、音效、3-4 人局、前端回放播放器、自弈强化学习。
